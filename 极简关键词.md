
一、综合概述（高频）
自我介绍
6 年前端、React+TS、中后台、组件库、工程化、AI 研发工作流平台、Agent Loop、RAG、可视化画布、SSE、Diff 审批、状态同步、提效 40%、样本统计、内部试用
平台 vs 普通 AI Chat
单轮问答、多轮执行循环、工作流编排、节点流转、Agent 工具调用、人工审批闭环、交付可落地代码
团队分工
前端架构主导、Agent 核心模块、ReactFlow、SSE、Diff 组件、协议设计、上下文压缩、安全规则、RAG 服务、Git 集成、流水线对接
工程架构 & 项目规划
Monorepo、纵向穿透、最小闭环、端到端验证、优先级排序、可演示
对标 Cursor/Copilot
个人编码助手、团队研发流水线、知识库能力、全流程自动化
求职动机 & 研发效能
AI + 研发效能、大规模工程场景、DORA 指标、交付速度、交付频率、质量、开发体验、自动化重复任务
二、AI Agent 核心技术（重中之重，必背）
Agent Loop
ReAct、while 循环、tool_calls、工具解析、参数执行、消息回填、多轮循环、双引擎降级、工具集、异常回填、最大轮次
死循环防控
轮数上限、失败计数器、重复行为检测、任务粒度拆分、终止总结、手动介入
上下文膨胀 & 压缩
Token 膨胀、源头截断、滑动窗口、摘要压缩、保留消息结构、tool_call_id 配对、Token 预算监控、内容分级截断、白名单
压缩策略选型
滑动窗口、LLM 摘要、Token 预算制、成本、延迟、稳定性、工程兜底
上下文跑偏兜底
分级压缩、快照回滚、异常检测、手动切换模式
长 Prompt 处理
阈值截断、动态调整保留轮数、Token 预算制
Token 优化 & 工具改造
write_file、全量输出、output token、edit_file、增量修改、patch 模式、降低 Token 消耗
tool result 优化
内容膨胀、差异化压缩、按工具类型处理、尾部截取
节点数据传递 & 一致性
结构化 JSON、execution.output、快照式数据、版本标记、过期提示、向后兼容
主流 Agent 架构
ReAct、CoT、Plan-and-Execute、Self-Reflection、Multi-Agent、Tool-Calling Loop、选型理由
模型路由
节点级路由、任务级规则路由、动态切换、上下文断层风险
Token 消耗 & 成本控制
Token 估算、上下文压缩、源头截断、模型分级、Prompt 约束
弃用 LangChain
重抽象、冗余概念、包体积、调试复杂、原生 SDK、轻量可控、定制化、依赖膨胀
MCP 集成
动态工具加载、工具分流、服务转发、安全白名单
Prompt & Skill 复用
模板复用、变量填充、预配置、降低使用门槛、全流程闭环
三、RAG 知识库（高频）
完整链路
文档解析、递归切片、Embedding、向量库、Milvus、COSINE 相似度、top-N 筛选、分值过滤、检索拼接、效果指标 Recall@5
切片策略
三层分割、段落 / 换行 / 字符切割、chunk 大小、字符重叠、语义边界、Markdown 适配
检索阈值
COSINE 分值、阈值选定、正负样本分布、精准率召回率曲线
RAG 精度优化
Rerank 精排、混合检索、BM25 稀疏检索、RRF 融合、Query 改写
向量库选型
Milvus、Chroma、pgvector、多租户、标量过滤、分区、持久化、索引 IVF_FLAT、nlist/nprobe、度量方式、双写一致性
Prompt Injection 防护
结构隔离、来源白名单、输出格式强约束、JSON Schema 校验、指令关键词检测
四、前端技术（高频）
ReactFlow 画布引擎
二次封装、nodes/edges 数据模型、nodeTypes/edgeTypes、自定义节点 / 连线、状态驱动样式、HTML5 拖拽、坐标转换、性能优化、useMemo、防抖保存、懒加载、React.memo
SSE 实时日志
单向推送、弃用 EventSource、fetch+ReadableStream、POST 鉴权、JWT、粘包 / 半包、缓冲区切割、断线重连、轮询兜底、AbortController、批量更新、requestAnimationFrame
SSE 断线恢复 & 续传
日志持久化、lastEventId、事件级续传、请求手动传参、去重、服务端落库
SSE 内存泄漏
组件销毁、页面卸载、主动终止请求、流关闭、清空缓存
Diff 审批
前置文件快照、目录扫描、增删改比对、react-diff-viewer、逐文件审批、Git 提交、创建 PR、快照回滚、代码隔离
虚拟滚动
可视区域渲染、动态高度、react-virtuoso、DOM 数量控制、滚动性能、followOutput
渲染性能优化
Web Worker、Diff 解析分片、requestIdleCallback、主线程防阻塞
状态管理 Zustand
轻量、无模板、无 Provider、体积小、全局状态划分、避免无效重渲染
iframe 微前端
样式隔离、shadow DOM、CSS Modules、命名空间、沙箱、postMessage、origin 校验、MessageBridge、msgId 请求响应配对、消息丢失 / 重复 / 超时、ready 握手、队列缓存、路由同步、popstate、isSyncing 防循环、高度自适应、弹窗处理、多 iframe 缓存 LRU
RBAC 权限系统
用户 - 角色 - 权限、页面级路由权限、按钮级权限指令、权限码 Set、O (1) 查询、DOM 移除、前后端双重校验、动态权限推送、WebSocket、轮询兜底、路由重校验
大文件上传
Web Worker、SparkMD5、文件 Hash、分片切片、File.slice、断点续传、秒传、p-queue 并发控制、分片重试、后端合并
五、前端工程化（高频）
Vite vs Webpack
按需 ESM、预构建 esbuild、HMR、冷启动、请求瀑布、Rollup 生产打包
pnpm 优势
硬链接、内容寻址、节省磁盘、杜绝幽灵依赖、pnpm workspace、catalog 版本统一
Monorepo 架构（Electron 场景）
单仓痛点、分包拆分（main/preload/renderer/shared）、职责解耦、workspace:*、TS 统一配置、esbuild/tsup、Vite、热更新、ESLint 跨层拦截、husky、lint-staged、构建提效
Electron 热更新 & 环境变量
electron-reloader、主进程局部重启、--mode 环境区分、.env 文件、cross-env、process.env、TS 类型定义
多包引用问题
optimizeDeps、link 模式、rollup 配置、路径别名、tsconfig 映射、HMR 修复
选型对比：pnpm/Lerna/Nx/Yarn
扁平化依赖、PnP、原生模块兼容、轻量、学习成本、onlyBuiltDependencies
preload 拆包原因
安全桥接、contextIsolation、API 白名单、生命周期解耦、独立校验
Electron IPC 类型安全
共享契约接口、泛型封装、通道名常量、编译期校验、tsc 全量检查
Electron 渲染进程安全
nodeIntegration 关闭、沙箱隔离、禁用 remote、IPC 通信替代、恶意脚本防护
Monorepo 迁移坑
跨层引用、循环依赖、代码梳理、公共代码抽离、ESLint 强制管控、分步迁移
架构收益
构建速度、Bug 减少、协作效率、代码复用、稳定性
内部脚手架
Commander、inquirer、EJS 模板、动态变量、条件渲染、递归创建文件、模板解耦、版本管理
组件库 TS 迁移
渐进式迁移、allowJs、第三方类型声明、declare.d.ts、模块扩展、类型断言
六、后端 & 架构（面试高频）
数据库设计
多租户、分层表结构、JSON 字段存储、日志表、外键取舍、向后兼容
DAG 并行执行
拓扑排序、入度表、Promise.all、barrier 汇聚、状态机、分支失败策略、结果合并
并发安全
防重复提交、前端 loading、后端内存锁、Redis 分布式锁、SET NX EX、Lua 脚本、AI API 限流、p-queue、执行幂等、executionId
Redis 应用场景
分布式锁、进度缓存、API 限流、任务队列、检索缓存、Bull 队列、Redis vs RabbitMQ
节点容错
分层异常捕获、工具自愈、节点状态标记、进程熔断、指数退避重试、全局超时熔断
CI/CD 集成
GitLab API、PR 创建、Webhook 监听、流水线状态回写、链路可视化
多租户设计
tenantId 逻辑隔离、Milvus 分区、Token 配额、队列优先级、租户配置、分级权限
节点插件化
插件 Schema、动态表单、HTTP 执行、版本管理、沙箱隔离、权限管控
七、安全防护（必问）
五层安全防护
命令白名单、路径穿越防护、参数注入、spawn 数组传参、执行超时、密钥隔离
XSS & CSRF
脚本注入、CSP、HTML 转义、HttpOnly Cookie、跨站请求伪造、SameSite、CSRF Token、Origin 校验、localStorage 风险
八、Electron 桌面端
进程分工
主进程、渲染进程、窗口管理、系统能力、IPC 通信、安全配置
模型部署链路
ONNX、TensorRT、MNN、进度解析、日志展示、产物管理
九、项目优化 & 总结（压轴追问）
现存问题 & 优化方向
edit_file 增量编辑、Zustand 状态管理、按需快照、Redis 分布式锁、BullMQ 队列、Rerank 精排、Token 预算制
高频追问速记
死循环处理、Token 溢出、并发问题、生产落地状态、重构优化方向